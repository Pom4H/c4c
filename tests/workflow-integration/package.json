{
  "name": "workflow-integration-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:workflows": "npx workflow build && node scripts/cjs-to-esm.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "workflow": "4.1.0-beta.54",
    "@workflow/world-local": "4.1.0-beta.30"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "vitest": "^3.2.4"
  }
}
